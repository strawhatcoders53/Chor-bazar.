# Architecture

> Auto-generated by /map on 2026-02-24

## Overview

Chor Bazzar is a premium streetwear e-commerce application with a high-end "cyberpunk/hacker" aesthetic. It features 3D product visualization, smooth kinetic scrolling, and a gamified "hacked" state.

```
┌─────────────────────────────────────────┐
│              [main.jsx]                 │
│          (React + Vite Entry)           │
├─────────────────────────────────────────┤
│         [Context Providers]             │
│   (Cart, Theme, Wishlist Contexts)      │
├─────────────────────────────────────────┤
│             [App Layer]                 │
│       (Router + Global Layout)          │
├─────────────────────────────────────────┤
│            [Pages Layer]                │
│ (Home, ProductList, ProductDetail, etc.)│
├─────────────────────────────────────────┤
│          [Component Layer]              │
│  (3D Viewer, HUD, Nav, Product Cards)   │
├─────────────────────────────────────────┤
│            [Data Layer]                 │
│ (Mock MongoDB/Stitch + products.json)   │
└─────────────────────────────────────────┘
```

## Components

### 3D Visualization
- **Purpose:** Renders interactive 3D shoe models.
- **Location:** `src/components/ShoeViewer3D.jsx`
- **Dependencies:** `three`, `@react-three/fiber`, `@react-three/drei`

### System HUD & Terminal
- **Purpose:** Provides a cyber-themed overlay and command palette.
- **Location:** `src/components/SystemHUD.jsx`, `src/components/AITerminal.jsx`
- **Dependencies:** `framer-motion`, `lucide-react`

### Boot Sequence
- **Purpose:** Immersive intro animation on first load.
- **Location:** `src/components/VaultBoot.jsx`, `src/components/BootSequence.jsx`

### State Management
- **Purpose:** Handles shopping cart, wishlist, and theme (hacked state).
- **Location:** `src/context/`

## Data Flow

1. **Initialization:** `BootSequence` runs, initializing global state.
2. **Browsing:** `ProductList` fetches data from `src/data/products.json` via simulated Stitch services.
3. **Interaction:** Users add items to `CartContext`, which persists state across pages.
4. **Hacked State:** `ThemeContext` manages the `isHacked` state, which triggers alternative styles and content across the app.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| MongoDB/Stitch | Mock API | Simulated backend for product retrieval and streaming |
| AI Service | API | Simulated AI-driven search and terminal commands |

## Technical Debt

- [ ] Technical Debt: No actual backend; uses simulated MongoDB/Stitch in `src/utils/stitch.js`.
- [ ] Technical Debt: Minimal error handling in `aiService.js` (fetch calls assume success).
- [ ] Technical Debt: Hardcoded API endpoints in `aiService.js`.

## Conventions

**Naming:** PascalCase for React components, camelCase for hooks and utils.
**Structure:** Component-based architecture with separate pages and context folders.
**Styling:** Tailwind CSS with custom neon/cyberpunk utility classes in `index.css`.
